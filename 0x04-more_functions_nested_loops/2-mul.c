#include "main.h"
/**
 * mul - func checks or checks for a digit (o through)
 * @a: a - variable
 * @b: b - variable
 * Return: always 0
 */
int mul(int a, int b)
{
	int mul;

	mul = a * b;
	return (mul);
}
