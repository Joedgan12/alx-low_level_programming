#include "main.h"

/**
 * main -prints its name
 * @argc: argument count
 * @argv: argument vector
 *
 * Return: always zero
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv);
	return (0);
}
